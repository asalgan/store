<div class="container">
	<div class="col6">
		<% @items.each do |item| %>
			<div>
		  <h1><%= item.name %></h1>
		<% end %>
			</div>

		<h1><%= @cart.total_quantity %></h1>
		<h1><%= @cart.total_price %></h1>
	</div>
</div>
